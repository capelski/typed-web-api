{
  "author": "Carles Capellas <capellas.carles@gmail.com>",
  "dependencies": {
    "@sample-express-app/common": "file:../common",
    "@typed-web-api/express": "^1.1.1",
    "express": "^4.18.2"
  },
  "description": "@typed-web-api/express usage samples",
  "devDependencies": {
    "nodemon": "^2.0.20",
    "typescript": "^5.3.2"
  },
  "license": "ISC",
  "name": "@sample-express-app/server",
  "private": true,
  "scripts": {
    "build": "tsc -p ./tsconfig.server.json",
    "build:check": "tsc -p ./tsconfig.server.json --noEmit",
    "clean": "rm -rf distribution",
    "clean-modules": "rm -rf node_modules",
    "dev": "nodemon source/sample-server.ts"
  },
  "version": "1.0.0"
}
